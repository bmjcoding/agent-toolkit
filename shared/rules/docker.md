# Docker Rules

Tool-agnostic directives for Dockerfile and docker-compose authoring.

> Claude Code specialization: `claude-code/rules/docker/docker.md`

## Rules

- Frontend and backend are always separate containers.
- Multi-layer builds, slim/alpine base images.
