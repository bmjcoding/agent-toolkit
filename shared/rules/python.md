# Python Rules

Tool-agnostic directives for Python projects.

> Claude Code specialization: `claude-code/rules/python/python.md`

## Rules

- Use `uv` for dependency management — never `pip install` directly.
- `uv sync --upgrade` to update dependencies.
