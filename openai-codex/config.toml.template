# Codex CLI project configuration template
# Version: Codex CLI v0.120.0
#
# Usage:
#   Copy this file to .codex/config.toml in a trusted project repo.
#   Global config lives at ~/.codex/config.toml.
#   Project config is only loaded from trusted repos (codex trust <dir>).

# ---------------------------------------------------------------------------
# Core settings
# ---------------------------------------------------------------------------

# Default model for all sessions (override per-agent in ~/.codex/agents/<n>.toml)
model = "codex-mini-latest"

# Sandbox mode controls filesystem and network access.
# Values: "workspace-write" | "read-only" | "full-network" | "danger-full-access"
sandbox_mode = "workspace-write"

# Approval policy for tool calls.
# Values: "auto" | "suggest" | "full-auto"
# "auto"       — approves safe read-only actions; prompts for writes/deletes
# "suggest"    — prompts before every tool call (safest)
# "full-auto"  — approves all tool calls without prompting (use with caution)
approval_policy = "auto"

# ---------------------------------------------------------------------------
# Skills configuration
#
# NOTE: skills.config[].path points to an individual skill folder, not a
# parent directory. Duplicate this [[skills.config]] block for each skill
# you want loaded.
#
# Codex discovers skills from (in order):
#   1. paths listed in [[skills.config]] entries below
#   2. .agents/skills/ at the repo root
#   3. $HOME/.agents/skills/
#   4. /etc/codex/skills/
#   5. bundled skills
#
# ---------------------------------------------------------------------------

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/backend"
description = "Lightweight backend workflow — implement with convention awareness, security review, and lint. Use when making backend-only changes without needing the full orchestrator pipeline."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/changelog"
description = "Canonical CHANGELOG.md standard: Keep a Changelog 1.1.0 + SemVer, required header, version sections, categories, per-component tags, comparison links, bump table. Use when creating or editing any CHANGELOG.md in the toolkit."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/design-authority"
description = "Design system guidance for generating and modifying frontend components. Provides token references, canonical patterns, and anti-convergence rules. Use when creating or editing React/Tailwind UI code."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/design-lint"
description = "Deterministic design system linting checks. Use when reviewing Tailwind CSS/React components for structural violations. Defines grep/regex patterns run by the design-linter agent."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/frontend"
description = "Lightweight frontend workflow — implement with design system enforcement, review, and lint. Use when making frontend-only changes without needing the full orchestrator pipeline."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/git-ship"
description = "Git shipping workflow — commit, push, open PRs, enable auto-merge, and clean up worktrees. Use when the user wants to ship code, open a PR, merge, or clean up branches."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/improve"
description = "Apply retro recommendations with automated verification. Use after /retro or anytime you want to improve a skill/agent. Supports --validate for autonomous improve-then-review validation cycles."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/infra"
description = "Lightweight infrastructure workflow — implement with SRE review for operational readiness. Use when making config, Docker, CI/CD, or infra changes without the full orchestrator pipeline."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/observability-patterns"
description = "Structured logging, health checks, metrics, and runbook patterns for SRE review and remediation. Use when reviewing services for operational readiness or remediating observability gaps."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/owasp-reference"
description = "OWASP Top 10, STRIDE threat modeling, and common vulnerability patterns for security review. Use when performing security review on backend code, APIs, or auth flows."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/prod-readiness"
description = "Full production readiness check — build, lint, audit, test, simplify, final validation, git verify, and ship verdict. Use when preparing code for production or before shipping."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/retro"
description = "Run a retrospective on any completed run — single agent, subagent, skill, or orchestration pipeline. Use when the user wants to debrief, analyze efficiency, or improve a workflow."

[[skills.config]]
path = "${AGENT_TOOLKIT_DIR}/skills/review-skill"
description = "Review a skill or agent definition for quality and correctness. Use when evaluating a contribution before merging, after writing a new skill, or when a skill underperforms. Supports batch review of directories with parallel dispatch."

# ---------------------------------------------------------------------------
# MCP server configuration (optional)
# ---------------------------------------------------------------------------
# Declare MCP servers as [mcp_servers.<id>] tables.
# Each server runs as a subprocess exposing tools to Codex sessions.
#
# Example:
# [mcp_servers.filesystem]
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
# env = {}
