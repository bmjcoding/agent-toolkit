{
  "name": "agent-toolkit",
  "private": true,
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": "^22.13.0 || >=24"
  },
  "overrides": {
    "smol-toml": "1.6.1"
  },
  "scripts": {
    "ci": "bash scripts/local-ci.sh",
    "sync": "node scripts/sync-canonical-adapters.js && node scripts/generate-index.js",
    "smoke": "node scripts/smoke-generated-assets.js",
    "check": "npm run lint && npm run lint:py && npm run validate:lifecycle && npm run validate:claude && npm run validate:definitions && npm run validate:evals && npm run validate:changelogs && npm run smoke",
    "validate:changelogs": "sh -c '{ git diff --name-only --diff-filter=ACMRD HEAD; git ls-files --others --exclude-standard; } | node scripts/validate-component-changelogs.js --files-from-stdin --require-release-version'",
    "validate:claude": "node scripts/validate-claude-assets.js",
    "validate:definitions": "python3 scripts/lint-definition.py skills && python3 scripts/lint-definition.py agents",
    "validate:evals": "python3 scripts/run-component-evals.py --require-component definition-review --require-component autoresearch-analyst",
    "validate:lifecycle": "node scripts/validate-lifecycle-metadata.js",
    "lint": "npm run lint:js && npm run lint:docs && npm run lint:data",
    "lint:fix": "npm run lint:js:fix && npm run lint:docs:fix && npm run lint:data:fix",
    "lint:js": "eslint .",
    "lint:js:fix": "eslint . --fix",
    "lint:docs": "markdownlint-cli2 \"**/*.md\"",
    "lint:docs:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "lint:data": "prettier --check \"**/*.{json,yaml,yml}\"",
    "lint:data:fix": "prettier --write \"**/*.{json,yaml,yml}\"",
    "lint:py": "uvx --from ruff==0.15.12 ruff check .",
    "lint:py:fix": "uvx --from ruff==0.15.12 ruff check . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "markdownlint-cli2": "^0.22.1",
    "prettier": "^3.9.6"
  }
}
