---
description: "Docker best practices: separate containers per service, multi-stage builds, slim/alpine base images."
applyTo: "**/Dockerfile,**/Dockerfile.*,**/docker-compose*.{yml,yaml}"
---
- Frontend and backend are always separate containers.
- Multi-layer builds, slim/alpine base images.
